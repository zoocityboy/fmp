name: Name
description: Yet another multi-package 

apps:
  - name: customer
    folder: capp
    description: Customer application
    stages:
      - fake
      - prod
    countries:
      - in
      - vn
      - ph
      - id
    exclude_packages:
      - mapp*
  - name: merchant
    folder: mapp
    description: Merchant application
    stages:
      - fake
      - prod
    countries:
      - in
      - vn
      - ph
      - id
    exclude_packages:
      - capp*
  - name: widget_catalog
    folder: widget_catalog
    description: Widget catalog
  - name: dynamic_forms_playground
    folder: dynamic_forms_playground
    description: Dynamic forms playground

packages:
  - packages/**
  - plugins/**