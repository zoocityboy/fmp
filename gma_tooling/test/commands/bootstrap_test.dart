import 'package:test/test.dart';

void main() {
  group('Bootstrap init', () {
    test(
        'When bootstrap over existing instance, then user can choose next step',
        () {},
        skip: true);
  });
  group('Bootstrap update', (){
    test(
        'When bootstrap over existing instance, then user can choose next step',
        () {},
        skip: true);
  });
}
