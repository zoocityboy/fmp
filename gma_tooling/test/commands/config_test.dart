import 'package:test/test.dart';

void main() {
  group('Config init', () {
    test(
        'When bootstrap over existing instance, then user can choose next step',
        () {},
        skip: true);
  });
  group('Config update', (){
    test(
        'When bootstrap over existing instance, then user can choose next step',
        () {},
        skip: true);
  });
}
